apiVersion: v1
kind: Secret
metadata:
  name: orchestrator-secret
  namespace: orchestrator
type: Opaque
stringData:
  # Replace the value below with a personal access token that has
  # permissions to trigger workflows in your repository. Do not commit
  # real secrets to version control.
  github_token: "REPLACE_WITH_YOUR_TOKEN"
